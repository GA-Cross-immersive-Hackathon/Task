<%=  render 'layouts/header' %>
<!-- foreach through list add listener and display name and est. time -->

<div class="container">

    <h1 class="col-sm-offset-3 col-sm-9 header-current-time">Job well done! </h1>

  <!-- if action is a priority -->

  <% @history.each do |action| %>
  <% if action.time_estimated === 60 %>
    <div class="action-div action-entire-list sixty col-sm-offset-2 col-sm-8 text-left" draggable="true" id="<%= action.id %>">
      <p class="x text-right"><%= link_to 'X', action, method: :delete, :remote => true, :class => 'delete_action' %></p>
      <h3 class="action" id="<%= action.id %>"><%= action.action_name%></h3>
      <p class= "time-foot text-right">Real Time: <%=action.time_took%></p>
      <div class="clicker-div">
        <p>Notes: <%= action.notes%></p>
        <p>Time: <%= action.time_estimated%></p>
      </div>
    </div>
  <% elsif action.time_estimated === 50 %>
    <div class="action-div action-entire-list fifty col-sm-offset-2 col-sm-8 text-left" draggable="true" id="<%= action.id %>">
      <p class="x text-right"><%= link_to 'X', action, method: :delete, :remote => true, :class => 'delete_action' %></p>
      <h3 class="action" id="<%= action.id %>"><%= action.action_name%></h3>
      <p class= "time-foot text-right">Real Time: <%=action.time_took%></p>
      <div class="clicker-div">
        <p>Notes: <%= action.notes%></p>
        <p>Time: <%= action.time_estimated%></p>
      </div>
    </div>
  <% elsif action.time_estimated === 40 %>
    <div class="action-div action-entire-list forty col-sm-offset-2 col-sm-8 text-left" draggable="true" id="<%= action.id %>">
      <p class="x text-right"><%= link_to 'X', action, method: :delete, :remote => true, :class => 'delete_action' %></p>
      <h3 class="action" id="<%= action.id %>"><%= action.action_name%></h3>
      <p class= "time-foot text-right">Real Time: <%=action.time_took%></p>
      <div class="clicker-div">
        <p>Notes: <%= action.notes%></p>
        <p>Time: <%= action.time_estimated%></p>
      </div>
    </div>
  <% elsif action.time_estimated === 30 %>
    <div class="action-div action-entire-list thirty col-sm-offset-2 col-sm-8 text-left" draggable="true" id="<%= action.id %>">
      <p class="x text-right"><%= link_to 'X', action, method: :delete, :remote => true, :class => 'delete_action' %></p>
      <h3 class="action" id="<%= action.id %>"><%= action.action_name%></h3>
      <p class= "time-foot text-right">Real Time: <%=action.time_took%></p>
      <div class="clicker-div">
        <p>Notes: <%= action.notes%></p>
        <p>Time: <%= action.time_estimated%></p>
      </div>
    </div>
  <% elsif action.time_estimated === 20 %>
    <div class="action-div action-entire-list twenty col-sm-offset-2 col-sm-8 text-left" draggable="true" id="<%= action.id %>">
      <p class="x text-right"><%= link_to 'X', action, method: :delete, :remote => true, :class => 'delete_action' %></p>
      <h3 class="action" id="<%= action.id %>"><%= action.action_name%></h3>
      <p class= "time-foot text-right">Real Time: <%=action.time_took%></p>
      <div class="clicker-div">
        <p>Notes: <%= action.notes%></p>
        <p>Time: <%= action.time_estimated%></p>
      </div>
    </div>
  <% elsif action.time_estimated === 10 %>
    <div class="action-div action-entire-list ten col-sm-offset-2 col-sm-8 text-left" draggable="true" id="<%= action.id %>">
      <p class="x text-right"><%= link_to 'X', action, method: :delete, :remote => true, :class => 'delete_action' %></p>
      <h3 class="action" id="<%= action.id %>"><%= action.action_name%></h3>
      <p class= "time-foot text-right">Real Time: <%=action.time_took%></p>
      <div class="clicker-div">
        <p>Notes: <%= action.notes%></p>
        <p>Time: <%= action.time_estimated%></p>
      </div>
    </div>
  <% elsif action.time_estimated === 5 %>
    <div class="action-div action-entire-list five col-sm-offset-2 col-sm-8 text-left" draggable="true" id="<%= action.id %>">
      <p class="x text-right"><%= link_to 'X', action, method: :delete, :remote => true, :class => 'delete_action' %></p>
      <h3 class="action" id="<%= action.id %>"><%= action.action_name%></h3>
      <p class= "time-foot text-right">Real Time: <%=action.time_took%></p>
      <div class="clicker-div">
        <p>Notes: <%= action.notes%></p>
        <p>Time: <%= action.time_estimated%></p>
      </div>
    </div>
  <% end %>
  <% end %>
</div>
<script charset="utf-8">
  collapsableList();
</script>
